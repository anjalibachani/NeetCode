class Solution {
    public int[] replaceElements(int[] arr) {
        for(int i = 0; i < arr.length - 1; i++) {
            int high = 0;
            for(int j = i+1; j < arr.length; j++) {
                if(arr[j] > high) {
                    high = arr[j];
                }      
            }
            arr[i] = high;
        }
        arr[arr.length - 1] = -1;
        return arr;
    }
}